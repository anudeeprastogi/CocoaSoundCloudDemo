platform :ios, "4.3"

xcodeproj 'SoundShare/SoundShare.xcodeproj'
####
# Please always specify an absolute version, or sha with each pod since we don't check in Podfile.lock & the Pods directory

# Dependencies on JSONKit, OHAttributedLabel and NXOauth2Client are defined in CocoaSoundCloudAPI and CocoaSoundCloudUI podspec
pod 'CocoaSoundCloudAPI', '~>1.0.1'
pod 'CocoaSoundCloudUI', '~>1.0.5'
